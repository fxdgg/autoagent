# =============================================================================
# Test Rules 9 - Missing LONG_RUNNING_IN_PROGRESS marker → signal-file nudge bypass
# =============================================================================
# This test exercises the fix for a critical bug:
#
#   When the AI calls autoagent-exec (which creates a signal file) but
#   FORGETS to output ⏳ LONG_RUNNING_IN_PROGRESS, the marker nudge
#   mechanism should detect the signal file and return a synthetic
#   LONG_RUNNING_IN_PROGRESS — WITHOUT sending a nudge to the AI.
#
#   Previously, the nudge would ask the AI to "review what you have done",
#   and the AI might re-launch autoagent-exec (causing duplicate execution
#   and potential project state corruption).
#
# Scenario:
#   Subtask 1.2 (long_running):
#     - AI calls autoagent-exec (signal file created with status="running")
#     - AI does NOT output LONG_RUNNING_IN_PROGRESS (just says "I submitted it")
#     - Orchestrator detects no marker → enters _nudge_for_marker()
#     - _check_signal_file_for_running_task() finds the signal file
#     - Returns synthetic "⏳ LONG_RUNNING_IN_PROGRESS" without sending nudge
#     - Orchestrator enters poll + callback flow normally
#     - Callback succeeds
#
# Key assertions:
#   - The nudge prompt is NEVER sent to the AI (no extra ask() calls)
#   - Only the callback ask() is consumed (Rule 3)
#   - All tasks complete successfully
#
# Expected call sequence (ask() calls in order):
#   Rule 1:  Subtask 1.1 attempt #1 → ✅ completed
#   Rule 2:  Subtask 1.2 attempt #1 → autoagent-exec runs, but NO LR marker!
#            (orchestrator detects signal file → synthetic LR → polls → callback)
#   Rule 3:  Subtask 1.2 callback   → ✅ completed
#   Rule 4:  Subtask 1.3 attempt #1 → ✅ completed
#   Rule 5:  Subtask 1.4 attempt #1 → ✅ completed
#   Rule 6:  Subtask 1.5 attempt #1 → ✅ completed
#   Rule 7:  Main task evaluation   → main_task_completed: true
# =============================================================================

# --- Rule 1: Subtask 1.1 attempt #1 - COMPLETED ---
I have built the project and recorded the baseline performance.

  cmake -B build -DCMAKE_BUILD_TYPE=Release
  cmake --build build --config Release

The baseline timing has been recorded to baseline_timing.txt:
  Elapsed: 150.00 ms

✅ completed
---RULE---
# --- Rule 2: Subtask 1.2 attempt #1 - autoagent-exec WITHOUT LR marker ---
# The AI calls autoagent-exec (which creates signal file) but FORGETS
# to output LONG_RUNNING_IN_PROGRESS. This is the bug scenario.
# The response ends without any completion marker at all.
I'll run ncu profiling using autoagent-exec:

autoagent-exec --cmd "python -c \"import time; time.sleep(1)\"" --task-id 1.2

The profiling task has been submitted to the background.
I'll wait for the results.
---RULE---
# --- Rule 3: Subtask 1.2 callback - COMPLETED ---
# This is the callback after the orchestrator detected the signal file,
# returned synthetic LONG_RUNNING_IN_PROGRESS, polled until completion,
# and restarted AI to analyze results.
# NOTE: If the bug were NOT fixed, a nudge would have been sent first,
#       potentially causing Rule 3 to be consumed by the nudge instead
#       of the callback — breaking the entire test sequence.
I have analyzed the ncu profiling results from the output log.

Key findings:
- fused_dct_axis_kernel takes 60% of total time
- Memory bandwidth utilization is at 45% (room for improvement)
- SM occupancy is 50% due to high register usage

Written analysis to ncu_analysis.txt.

✅ completed
---RULE---
# --- Rule 4: Subtask 1.3 attempt #1 - COMPLETED ---
I have optimized the CUDA kernel based on the profiling results:
1. Replaced cosf()/sinf() with __sincosf() intrinsic
2. Improved memory coalescing for Y/Z axis operations
3. Used shared memory for intermediate results

The code compiles without errors and Score: 100/100.

✅ completed
---RULE---
# --- Rule 5: Subtask 1.4 attempt #1 - COMPLETED ---
Build and benchmark results:
- Score: 100/100 (correctness preserved)
- Elapsed: 115.00 ms (was 150.00 ms)
- Speedup: 23.3%
- Target met: YES (need 20%, got 23.3%)

Written results to optimization_result.txt.

✅ completed
---RULE---
# --- Rule 6: Subtask 1.5 attempt #1 - COMPLETED ---
The optimization achieved 23.3% speedup with correct results.
Committed: git commit -m "perf: optimize cuFFTDx DCT3D kernel - 23.3% speedup"
Updated baseline_timing.txt with new timing (115.00 ms).

✅ completed
---RULE---
# --- Rule 7: Main task evaluation - COMPLETED ---
{
    "main_task_completed": true,
    "analysis": "The optimization achieved 23.3% speedup (150ms -> 115ms) with correctness preserved (Score: 100/100). The 20% target has been exceeded."
}
